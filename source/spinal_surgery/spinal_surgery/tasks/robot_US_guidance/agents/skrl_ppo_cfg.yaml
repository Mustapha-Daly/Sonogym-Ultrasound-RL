seed: 42 # make training reproducible


# Models are instantiated using skrl's model instantiator utility
# https://skrl.readthedocs.io/en/latest/api/utils/model_instantiators.html
models:
  separate: False # actor and critic share the same CNN backbone if False
  policy:  # see gaussian_model parameters
    class: GaussianMixin # CategoricalMixin #probability distribution  over 4 actions, not a fixed action to allow exploration through randomness ( go 2mm-- with std 0.5> agent goes somewhere between 1.8 and 2.3)
    clip_actions: False # enforce limit for actions to avoid out of range exploration (e.g., due to high std)
    clip_log_std: True # to enforce log limits
    min_log_std: -3.0
    max_log_std: 2.0 # was 1.0 2.0 matches paper
    initial_log_std: 0.0 # was exp(-1.0) = 0.3679, so initial std = 0.3679, which was too conservative for exploration, so switched to  original 0.0 making std at init 1.0
    network:
      - name: features_extractor # shared CNN backbone to extract features from input states (e.g., images)
        input: STATES  # PyTorch NHWC -> NCHW 
        layers: #extract visual features 
          - conv2d: {out_channels: 32, kernel_size: 8, stride: 2, padding: 0}
          - conv2d: {out_channels: 64, kernel_size: 4, stride: 2, padding: 0}
          - conv2d: {out_channels: 64, kernel_size: 4, stride: 2, padding: 0}
          - conv2d: {out_channels: 64, kernel_size: 3, stride: 1, padding: 0}
          - flatten
        activations: relu
      - name: net
        input: features_extractor
        layers: [512, 256, 128]
        activations: elu
    output: ACTIONS
  value:  # see deterministic_model parameters #critic is deterministic because it only estimates the value of a state, no need for exploration
    class: DeterministicMixin # outputs a fixed value for each state
    clip_actions: False
    network:
      - name: features_extractor
        input: STATES  # PyTorch NHWC -> NCHW
        layers:
          - conv2d: {out_channels: 32, kernel_size: 8, stride: 2, padding: 0}
          - conv2d: {out_channels: 64, kernel_size: 4, stride: 2, padding: 0}
          - conv2d: {out_channels: 64, kernel_size: 4, stride: 2, padding: 0}
          - conv2d: {out_channels: 64, kernel_size: 3, stride: 1, padding: 0}
          - flatten
        activations: relu
      - name: net
        input: features_extractor
        layers: [512, 256, 128]
        activations: elu
    output: ONE # one value for each state


# Rollout memory
# https://skrl.readthedocs.io/en/latest/api/memories/random.html
memory:
  class: RandomMemory
  memory_size: -1  # automatically determined (same as agent:rollouts)


# PPO agent configuration (field names are from PPO_DEFAULT_CONFIG)
# https://skrl.readthedocs.io/en/latest/api/agents/ppo.html
agent:
  class: PPO
  rollouts: 720 # must match episode length in sim which is 720 currently
  learning_epochs: 4 # match paper exactly now: reuses the buffer 4 times, 720×4 envs/5 = 576 samples/epoch
  mini_batches: 12 # buffer = 720 × 8 = 5760 samples was 48*5 epochs=240 gradient steps per rollout was too much for KlAdaptiver and crashed LR, changed to 12 to have only 48 gradient steps, tried 6 but did not work because of memory
  discount_factor: 0.99
  lambda: 0.95
  learning_rate: 1.0e-04 # same as paper
  learning_rate_scheduler: KLAdaptiveLR #we put it back
  learning_rate_scheduler_kwargs:
    kl_threshold: 0.02 # was 0.008 (paper) — raised because 48 grad steps/rollout accumulates KL > 0.016 consistently, crashing LR to min floor
    max_lr: 4.0e-04
    min_lr: 1.0e-05 #
  state_preprocessor: null
  state_preprocessor_kwargs: null

  value_preprocessor: RunningStandardScaler
  value_preprocessor_kwargs: null
  random_timesteps: 0
  learning_starts: 0
  grad_norm_clip: 1.0
  ratio_clip: 0.2 # double what i had before, 0.1 was too small for this task, 0.2 is standard in PPO paper
  value_clip: 0.2
  clip_predicted_values: True
  entropy_loss_scale: 0.001 # instead of 0.005 to encourage exploration
  value_loss_scale: 1.0 # Set to 1.0 to match the PPO paper, was 2.0 but rewards are already big
  kl_threshold: 0.016
  rewards_shaper_scale: 1.0
  time_limit_bootstrap: True 
  # logging and checkpoint
  experiment:
    directory: "US_guidance"
    experiment_name: "PPO_US"
    write_interval: 200
    checkpoint_interval: 5000



# Sequential trainer
# https://skrl.readthedocs.io/en/latest/api/trainers/sequential.html
trainer:
  class: SequentialTrainer
  timesteps: 100000 
  environment_info: log
probe:   #i added this part
  type: convex          # <<< KEY
  #radius: 60             # mm, typical convex probe
  fan_angle: 60          # degrees (50–90 common)
  depth: 0.16            # mm (matches image height)
